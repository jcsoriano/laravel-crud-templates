<?php

namespace App\Http\Resources{{ NAMESPACE_PATH }};

{{ NAMESPACES }}

class {{ MODEL }}Resource extends JsonResource
{
    /**
     * Transform the resource into an array.
     *
     * @return array<string, mixed>
     */
    public function toArray(Request $request): array
    {
        return [
            ...$this->only([
                {{ RESOURCE_ONLY }}
            ]),
            {{ RESOURCE_RELATIONS }}
        ];
    }
}
