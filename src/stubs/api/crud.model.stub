<?php

namespace App\Models{{ NAMESPACE_PATH }};

{{ NAMESPACES }}

class {{ MODEL }} extends Model
{
    use HasFactory, SoftDeletes;

    protected $fillable = [
        {{ FILLABLE }}
    ];

{{ CASTS }}

{{ RELATIONS }}
}
